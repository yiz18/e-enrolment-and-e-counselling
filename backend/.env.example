# Copy this file to .env and customise as needed.

# Comma-separated list of allowed CORS origins.
# Use * to allow any origin (fine for local dev; tighten for production web clients).
CORS_ORIGINS=*

# Local dev only — Render sets PORT automatically.
# PORT=8000
